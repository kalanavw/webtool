import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'webtool';
//image crop, convert the extension, parse into pdf, pdf into images
// image in to base64, base64 into image
//base64 generation in every language
//interview question


// QR generator
}


 // npm run ng serve -- --host=0.0.0.0 --disable-host-check